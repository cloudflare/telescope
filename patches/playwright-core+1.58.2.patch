diff --git a/node_modules/playwright-core/lib/server/videoRecorder.js b/node_modules/playwright-core/lib/server/videoRecorder.js
index b042fb6..12f684c 100644
--- a/node_modules/playwright-core/lib/server/videoRecorder.js
+++ b/node_modules/playwright-core/lib/server/videoRecorder.js
@@ -23,6 +23,7 @@ __export(videoRecorder_exports, {
 module.exports = __toCommonJS(videoRecorder_exports);
 var import_utils = require("../utils");
 var import_processLauncher = require("./utils/processLauncher");
+var import_utilsBundle = require("../utilsBundle");
 const fps = 25;
 class VideoRecorder {
   constructor(ffmpegPath, options) {
@@ -35,6 +36,7 @@ class VideoRecorder {
     this._frameQueue = [];
     this._isStopped = false;
     this._ffmpegPath = ffmpegPath;
+    this._options = options;
     if (!options.outputFile.endsWith(".webm"))
       throw new Error("File must have .webm extension");
     this._launchPromise = this._launch(options).catch((e) => e);
@@ -105,8 +107,10 @@ class VideoRecorder {
     const error = await this._launchPromise;
     if (error)
       throw error;
-    if (this._isStopped || !this._lastFrame)
+    if (this._isStopped)
       return;
+    if (!this._lastFrame)
+      this._writeFrame(createWhiteImage(this._options.width, this._options.height), (0, import_utils.monotonicTime)());
     const addTime = Math.max(((0, import_utils.monotonicTime)() - this._lastWriteNodeTime) / 1e3, 1);
     this._writeFrame(Buffer.from([]), this._lastFrame.timestamp + addTime);
     this._isStopped = true;
@@ -118,6 +122,10 @@ class VideoRecorder {
     }
   }
 }
+function createWhiteImage(width, height) {
+  const data = Buffer.alloc(width * height * 4, 255);
+  return import_utilsBundle.jpegjs.encode({ data, width, height }, 80).data;
+}
 // Annotate the CommonJS export names for ESM import in node:
 0 && (module.exports = {
   VideoRecorder
