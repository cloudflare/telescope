{
  "name": "telescopetest-io",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "cross-env CLOUDFLARE_ENV=development astro dev",
    "dev:setup": "./scripts/dev-setup.sh",
    "dev:clean": "rm -rf .wrangler .env",
    "build": "cross-env CLOUDFLARE_ENV=development astro build",
    "preview": "astro preview",
    "cf-typegen": "wrangler types",
    "deploy": "cross-env CLOUDFLARE_ENV=production astro build && wrangler deploy --env production",
    "deploy:staging": "cross-env CLOUDFLARE_ENV=staging astro build && wrangler deploy --env staging",
    "migrate:staging": "npx wrangler d1 migrations apply telescope-db-staging --remote --env staging",
    "migrate:local": "npx wrangler d1 migrations apply telescope-db-development --local --env development",
    "generate": "prisma generate",
    "studio": "prisma studio"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.1.3",
    "@astrojs/react": "^5.0.1",
    "@phosphor-icons/react": "^2.1.10",
    "@prisma/adapter-d1": "^7.5.0",
    "@prisma/client": "^7.5.0",
    "astro": "^6.0.8",
    "fflate": "^0.8.2",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^24.12.0",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "cross-env": "^10.1.0",
    "dotenv": "^17.2.4",
    "prisma": "^7.5.0",
    "wrangler": "^4.61.0"
  },
  "engines": {
    "node": ">=24",
    "npm": ">=11.9.0"
  }
}
